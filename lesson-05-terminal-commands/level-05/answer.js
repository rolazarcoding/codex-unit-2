// Level 05 — Parent directory (..)
// TODO: Use `cd ..` and/or `../` to reference sibling files; describe the steps here.
export default "The .. represents the parent folder. I can move to the parent folder with cd ..";
