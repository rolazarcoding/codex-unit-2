const challengeForm = document.getElementById("search-form");
const resultsEl = document.getElementById("results");
const errorEl9 = document.getElementById("error");
const paginationEl = document.getElementById("pagination");

if (challengeForm) {
  challengeForm.addEventListener("submit", async (e) => {
    // TODO: preventDefault, construct request URL/body, perform fetch
    // TODO: render results, implement simple pagination/filtering, show errors
    console.log("level-09 challenge placeholder");
  });
}
